extends ../layout

block content
    h1.
        Blog Instance

    h3= detail.title

    h5= detail.content

    p= " Created by " + detail.created_by.name

    a(href='/blog/'+detail._id+'/edit')= " Edit "